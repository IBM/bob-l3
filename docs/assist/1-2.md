**MODULE 1 | ASSIST**
# **LITERATE CODING**

---

## **i. Editing with intent**

---

Tab completion and next edit prediction shine while you are writing *new* code. But much of a developer's day is spent making precise, surgical change to code that already exists. To that end, **literate coding** is the tool of choice. It lets you annotate— using plain, natural language instructions — direct inside of a file where you needed the changes to occur. Bob then takes care of the heavy lifting by ensuring that every change is accounted for and that no instructions are missed. 

IBM Bob reads the instruction in place and produces a targeted, localized modification. The developer is still describing intent in plain language; the difference is that here you are pointing at a specific spot and saying "change *this*." Think of it like annotating changes in a collaborative Word document, where you can delegate the modifications to a trusted colleague who will shoulder the workload.

> **Watch first:** [Chapter 2.2: Literate Coding \[IBM Bob L3\]](https://ibm.seismic.com/Link/Content/DCC8CVWmQWGbc8hJ36j3qJ48XfB3)
>
> Maximilian Jesch *(Outbound Product Manager — IBM Bob)* demonstrates Bob's literate coding capabilities. *\[2 min\]*

---

## **ii. Why this matters**

---

Literate coding is at its best for focused, in-place work:

- **Wrapping logic** around existing code — retry mechanisms, error handling, or logging
- **Refactoring a specific block** while leaving the surrounding logic untouched
- **Quick, targeted modifications** to a single function without disturbing the broader structure

IBM Bob users engage the literate coding mode through the magic wand icon in the Bob IDE (you'll see exactly how this is performed in a few moments.) Once active, it allows those users to write natural-language instructions at a precise code location. They can target the exact section of code where changes are to be made; alternatively, they can queue up several instructions in sequence, across multiple locations of a single file.

!!! note "AI MATURITY CURVE"

    Literate Coding is a prime example of where the *control* that the human in the loop retains with IBM Bob — as part of the **early Assist stage along the AI maturity curve** — becomes tangible and truly valuable. IBM Bob never silently rewrites your files or codebase. Instead, it presents every change in a *diff* view that shows the added code, the removed code, and enough surrounding context to understand the scope of what is changing. You review, then you decide on whether to implement the suggestion or disregard it. Nothing is committed until you say so.

**How it works**

The workflow is deliberately simple:

1. Position your cursor within the codebase where the change belongs
2. Click the magic wand icon to enter literate coding mode
3. Type a natural-language instruction describing the change you want to implement
4. For changes that span several places, add an instruction line at each location
5. Click generate to produce the modifications (across one or many instructions)

---

**Going further**

For sweeping, multi-file changes or architectural restructuring, you will instead want to reach for IBM Bob's **Code mode**. The topic is covered extensively within <a href="https://ibm.github.io/bob-l3/delegate/2-1.md" target="_blank">**MODULE 2 | DELEGATE**</a>, which follows this section.

!!! warning "**EXPECT SOME VARIABILITY**"
    
    Generative AI is *non-deterministic* and as such your results may differ from what you see here. If an output diverges, consider further refining the prompt, following up with another prompt, or adjusting the code by hand. Iteration and further precision are often the best remedies.


## **iii. Hands-on: adding retry logic with Literate Coding**

---

> **Follow the video** for a full walkthrough: [Chapter 2.2: Literate Coding \[IBM Bob L3\]](https://ibm.seismic.com/Link/Content/DCC8CVWmQWGbc8hJ36j3qJ48XfB3)

Galaxium's backend can be slow to respond when fetching flight information. If you test on this, you'll discover that occasional timeouts are often the result. This underforming feature is an ideal candidate for showcasing the small, surgical fixes that literate coding excels at.

In the following section, you will apply literate coding to wrap the flight-fetching call in `retry` logic — and do so with minimal coding work on your part.

---

1. Within the Bob IDE, open the **Flights.tsx** file by navigating to the following directory location:

    !!! warning "**Filepath:**"
        **/**`booking_system_frontend`**/**`src`**/**`pages`**/** `Flights.tsx`

    <p align="left">
    <img src="https://colony-recorder.s3.amazonaws.com/files/2026-06-01/47057405-de4e-4a16-b733-add7692b5acb/File_2c52e62792b84d49b1699cd7054f488e_text_export.jpeg" alt="" width="600" />
    </p>

---

2. Click the **magic wand icon** near the top of the editor (or press ++ctrl++ + ++i++) to activate literate coding mode.

    <p align="left">
    <img src="https://colony-recorder.s3.amazonaws.com/files/2026-06-01/541203d9-0a20-4ac7-a8dd-9a60d2334373/File_70a4346deb6344bc9763d181f6527c5a_text_export.jpeg" alt="" width="600" />
    </p>

---

3. A bar will at the bottom-center of the UI with two arrows, **Clear All** and **Generate**, to indicate that you have now activated literate coding mode. Treat the visible mode state and the Generate button as your primary indicators, since the exact highlight color can vary by release.

    - On **line 43** of `Flights.tsx`, copy and paste the following statement:

        ```
        the backend is unreliable at times. We need a retry logic here
        ```

    - The text should highlight to show that the literate coding mode is active.

    - Unlike the Tab Completion feature showcased in the previous section, with Literate Coding there is no need to *"comment out"* the instruction sets.

    <p align="left">
    <img src="https://colony-recorder.s3.amazonaws.com/files/2026-06-01/5432d099-5b11-47f9-a082-51d3b091d26b/File_785d53ed61c64f91ab8f23c788b93847_text_export.jpeg" alt="" width="600" />
    </p>

---

4. With the instruction set now inserted at the desired location, click the **Generate** button.

    <p align="left">
    <img src="https://colony-recorder.s3.amazonaws.com/files/2026-06-01/fe0914c5-7e42-49c1-8105-8ecdd2848ab0/File_5cc62eeb2b7f4944a980d4077d2916eb_text_export.jpeg" alt="" width="600" />
    </p>

---

5. A ***diff* view** will render in-line at the insertion point.
    
    - Code that IBM Bob is proposing to *remove* will be highlighted in red.
    
    - Code that IBM Bob is proposing to *add* will be highlighted in a contrasting color. With some builds of the Bob IDE it may be highlighted in yellow, in other versions it may show as green — the key thing is that it is *not* red.

    - Review the on-screen *diff* carefully before **accepting** the proposed changes. Note that your generated code may differ slightly from the code documented here.

    === "Flights.tsx"

        ``` tsx linenums="57"
        if (retryCount < MAX_RETRIES) {
            // Retry after delay
            toast.error('Failed to load flights. Retrying... (${retryCount + 1}/${MAX_RETRIES})');
            await new Promise(resolve => setTimeout(resolve, RETRY_DELAY * (retryCount + 1)));
            return loadFlights(retryCount + 1);
        } else {
            toast.error('Failed to load flights after multiple attempts');
            console.error(error);
        }
        ```

    <p align="left">
    <img src="https://colony-recorder.s3.amazonaws.com/files/2026-06-01/45350cda-c2c2-4510-ae2f-97d0d781697e/File_797886930dc94807af2f42b9a63b4b0c_text_export.jpeg" alt="" width="600" />
    </p>

---

6. If everything looks fine, press **Accept All** to confirm the proposed code changes.

    - All red lines are automatically removed and the new (color-highlighted) lines are written into the file.

    - This particular change may not produce anything visible within the running app — and that is by design. The point is to watch targeted code generation happen *in context* against real code.

    <p align="left">
    <img src="https://colony-recorder.s3.amazonaws.com/files/2026-06-01/4ef4a490-d834-4a35-bc3c-9e05d7837ac2/File_cafa1952f83648459de15ff4d8cca923_text_export.jpeg" alt="" width="600" />
    </p>

---

## **iv. Key takeaways**

---

- Literate coding turns plain-language instructions, written in place, into precise code change.
- The *diff* view keeps you in full control, allowing you to inspect and understand every change before it is committed to code.
- The literate coding mode is most effective for focused, single-file modifications.
- For multi-file or architectural changes, Code mode is the more appropriate tool for the job — and the topic you'll explore next.