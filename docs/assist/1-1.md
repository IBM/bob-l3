**MODULE 1 | ASSIST**
# **LITERATE CODING**

---

## **i. Editing with intent**

---

Much of a developer's day is spent making precise, surgical change to code that already exists. **Literate Coding** allows developer and users to annotate their code— using plain, natural language instructions —directly inside and inline the files they are working with. Bob then takes care of the heavy lifting by ensuring that every change is accounted for and that no instructions are missed. 

IBM Bob reads the instruction in place and produces a targeted, localized modification. The developer is still describing intent in plain language; the difference is that here you are pointing at a specific spot and saying "change *this*." Think of it like annotating changes in a collaborative Word document, where you can delegate the modifications to a trusted colleague who will shoulder the workload.

This is the first (and most gentle) stage of the **AI Maturity Curve**, and therefore a natural sweet spot for building your understanding of IBM Bob. Across this module, you'll work shoulder to shoulder with Bob from the perspective of a software developer paired with the Bob editor. As you work, Bob will be anticipating the next keystroke, drafting the next few lines, and making targeted edits upon your request — all the while keeping the developer (*you*) in control of accepting or rejecting every AI-generated proposed change.

![](_attachments/Module-1-Assist.png)
    
!!! note "AI MATURITY CURVE"
    Literate Coding is a prime example of where the *control* that the human in the loop retains with IBM Bob — as part of the **early Assist stage along the AI Maturity Curve** — becomes tangible and truly valuable. IBM Bob never silently rewrites your files or codebase. Instead, it presents every change in a *diff* view that shows the added code, the removed code, and enough surrounding context to understand the scope of what is changing. You review, then you decide on whether to implement the suggestion or disregard it. Nothing is committed until you say so.

## **ii. Why this matters**

---

Literate coding is at its best for focused, in-place work:

- **Wrapping logic** around existing code — retry mechanisms, error handling, or logging
- **Refactoring a specific block** while leaving the surrounding logic untouched
- **Quick, targeted modifications** to a single function without disturbing the broader structure

IBM Bob users engage the literate coding mode through the magic wand icon in the Bob IDE (you'll see exactly how this is performed in a few moments.) Once active, it allows those users to write natural-language instructions at a precise code location. They can target the exact section of code where changes are to be made; alternatively, they can queue up several instructions in sequence, across multiple locations of a single file.

---

**How it works**

The workflow is deliberately simple:

1. Position your cursor within the codebase where the change belongs
2. Click the magic wand icon to enter literate coding mode
3. Type a natural-language instruction describing the change you want to implement
4. For changes that span several places, add an instruction line at each location
5. Click generate to produce the modifications (across one or many instructions)

---

## **iii. Adding email address validation checks**

---

Let's begin by adding to *Galaxium Travels*' set of capabilities a new feature that's currently missing: validation of email addresses that a user types when they sign in.

---

1. Click the **Explorer** tab along the left edge of the Bob IDE.

    From the previous **<a href="https://ibm.github.io/bob-l3/setup/" target="_blank">Setup</a>** module instructions, the `galaxium-travels` directory should already be open inside the *Explorer* tab.

    <p align="left">
    <img src="https://colony-recorder.s3.amazonaws.com/files/2026-06-01/dc6e0bb9-8bc5-444b-aae7-c8ef5eb02b33/File_d58a9ab512c241e9ae42c81d4e710a29_text_export.jpeg" alt="" width="600" />
    </p>
    ??? warning "CAN'T LOCATE THE DIRECTORY?"
        If this is not the case:
        
        - Click **Open Folder** to expose the *File Explorer* view
        - Drill down into the `galaxium-travels` folder
    
        <p align="left">
        <img src="https://colony-recorder.s3.amazonaws.com/files/2026-06-01/50832c90-ba15-40ca-b57c-2611e7902f6f/File_ad5aec0080304e5d9114f804aa079dd2_text_export.jpeg" alt="" width="600" />
        </p>
        <p align="left">
        <img src="https://colony-recorder.s3.amazonaws.com/files/2026-06-01/50787f12-e1dd-410b-89f0-145df966f8c8/File_68c498ba382f4b529983a0a0980f01ab_text_export.jpeg" alt="" width="600" />
        </p>

---

2. Drill down into the `galaxium-travels` and locate the `UserIdentification.tsx` file.

    It will be nestled inside the following directory hierarchy:
    !!! warning ""
        **/**`booking_system_frontend`**/**`src`**/**`components`**/**`user`**/**`UserIdentification.tsx`

    Double-click the file to open it within the editor.
    <p align="left">
    <img src="https://colony-recorder.s3.amazonaws.com/files/2026-06-01/688b5e85-07db-4269-bb3c-8dca639c135e/File_3ee96a2bb1684cfb9017cc440c241a21_text_export.jpeg" alt="" width="600" />
    </p>
    <p align="left">
    <img src="https://colony-recorder.s3.amazonaws.com/files/2026-06-01/01d1cb46-0ae0-4f02-872d-62824119a3ae/File_f9ca68765fed45b8a4af0d9a7ad8ec53_text_export.jpeg" alt="" width="600" />
    </p>

---

3. Within the file editor (the center panel of the Bob IDE), scroll down to the `UserIdentification` located on **line 13**.

    <p align="left">
    <img src="https://colony-recorder.s3.amazonaws.com/files/2026-06-01/74243ab6-12d1-48ad-a204-8a51bdc1911e/File_004b5c8a04414381ab2c503aff811b0c_text_export.jpeg" alt="" width="600" />
    </p>

---

In order to demonstrate IBM Bob's ability to generate functional application code based on basic, natural language specifications, we'll begin with a simple one-liner comment that describes the work we'd like the application to perform.

For this example, that will be validation of email addresses to ensure that users signing up for the Galaxium service are supplying compliant email addresses that follow the expected formatting.

---

4. Ensure that **Literate Coding Mode** is `ENABLED` by either toggling on the **magic wand tool** in the top-right corner of the `UserIdentification.tsx` editor (shown below).

    Alternatively, you can engage Literate Coding by pressing the ++command++ + ++i++ keyboard shortcut.
    ![](_attachments/literate-iii-4a.png)
    ![](_attachments/literate-iii-4b.png)

---

5. Within the `UserIdentification.tsx` file, place your cursor on **Line 19** of the file (just after the `UserIdentification` definition).

    Copy & paste the following comment onto **Line 19**. Make sure to include the blank (empty) lines *before* and *after* the `validate email addresses` comment.

    ```

    validate email addresses

    ```

    Before you click Generate, the setup within the file should appear as the following:


    === "UserIdentification.tsx"

        ``` tsx linenums="17" hl_lines="3 4 5"
        const [isLoading, setIsLoading] = useState(false);
        const [isNewUser, setIsNewUser] = useState(false);

        validate email addresses

        const handleSubmit = async (e: React.FormEvent) => {
            e.preventDefaut();
        }
        ```
    ??? warning "DEBUGGING: Error..."
        If IBM Bob returns an error when clicking **Generate** while *Literate Coding* mode is activated, double-check that your instruction comment `validate email addresses` does *NOT* include "comment out" annotations like double `//` or `#` lines. Recent iterations of Bob's LLMs have been documented generating errors (related to newString and oldString) when encountering these comment lines. 
    ![](_attachments/literate-iii-5.png)

---

6. After pasting the 3 new lines, look towards the bottom-center of the Bob IDE interface. Green and red-highlighted text will appear in-line with the code after pasting. Take note of the two Literate Coding options that have now appeared just below the highlighted text:

    - **Clear All**: selecting this option will disable Literate Coding on this code insert
    - **Generate**: selecting this option will pass the instruction to IBM Bob for code generation and modification
    
    Click the blue **Generate** button (or press ++command++ + ++return++) to execute.

    !!! warning "**EXPECT SOME VARIABILITY**"
        Generative AI is *non-deterministic* and as such your results may differ from what you see here. If an output diverges, consider further refining the prompt, following up with another prompt, or adjusting the code by hand. Iteration and further precision are often the best remedies.

---

7. IBM Bob will generate two new blocks of code within the `UserIdentification.tsx` file:

    - Across **Lines 20-22** (`isValidEmail` function)
    - Across **Lines 33-35** (`if(!isValidEmail ...` logic)

    === "UserIdentification.tsx"

        ``` tsx linenums="18" hl_lines="3 4 5 15 16 17 18"
        const [isNewUser, setIsNewUser] = useState(false);

        const isValidEmail = (value: string): boolean => {
            return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(value);
        };

        const handleSubmit = async (e: React.FormEvent) => {
            e.preventDefault();

            if (!name.trim() || !email.trim()) {
                toast.error('Please fill in all fields');
                return;
            }

            if (!isValidEmail(email.trim())) {
                toast.error('Please enter a valid email address');
                return;
            }

            setIsLoading(true); 
        ```
        ![](_attachments/literate-iii-7.png)

---

At this stage, the code suggestion is still *only* a suggestion: you, as the operator at the helm, have the option to: accept; modify; or reject and move on. As a reminder, because Bob's suggestions are non-deterministic, your generated function may look slightly different from the code documented. If the generated code does not compile, first compare the inserted function with the example shown in the lab documentation.

Note the colored highlighting, denoting IBM Bob's **diff** view:

- Green-highlighted text represents *new additions* to the code
- Red-highlighted text represents lines that have been *marked for removal* from the code
- The comment `validate email address` that was used as a natural language instruction for Bob will be *removed* if the generation recommendations are accepted

---

8. Click **Accept All** (or press ++command++ + ++return++) to confirm the changes to the code.

    Alternatively, you could click *Undo All* to reverse the changes, or use the up and down arrows to review the two new code blocks manually.
    ![](_attachments/literate-iii-8.png)

---

9. You will need to **reboot** the *Galaxium Travels* web app service for the changes to take effect.

    To do so:
    
    - To open a new Terminal window by click the **Terminal** button in the bottom-center portion of the Bob IDE interface *or* navigate using the top menu bar into **Terminal** > **New Terminal**
    - Within the Terminal window, locate and click the **+** icon, along the right-hand side of the Terminal window
    - This will launch a new Terminal connection into which you will next input additional commands
    
    <p align="left">
    <img src="https://colony-recorder.s3.amazonaws.com/files/2026-06-01/f3278573-5318-4443-b3d2-bc66c4a12015/File_35bca18c4b89427dbb017b9958d89dc0_text_export.jpeg" alt="" width="600" />
    </p>

---

10. The commands to be issued to the Terminal console vary according to the operating system your machine is running upon.

    From the expandable options below, follow and execute the instructions aligned to your machine's OS (either Mac/Linux or Windows). After completing the instructions within the appropriate tab, proceed to the next step in the lab guide.

    ??? quote "MAC OR LINUX"

        On macOS or Linux, run the following commands one at a time.

        **Step 1:**
        ``` bash
        cd ~/galaxium-travels
        ```
        
        **Step 2:**
        ``` bash
        ./start.sh
        ```
        
        You should see a *"Galaxium Travels is running!"* prompt returned to the console (amongst other messages) once the application has deployed successfully.

        </br>
        <p align="left">
        <img src="https://colony-recorder.s3.amazonaws.com/files/2026-06-01/6cf431b0-2db0-42a9-a43d-33b1abf1c3e5/File_852235e4905447ef9c691dbceaf81c0e_text_export.jpeg" alt="" width="600" />
        </p>

    ??? quote "WINDOWS OS"

        On Windows, run the following commands one at a time.

        **Step 1:**
        ``` bash
        cd $HOME\galaxium-travels

        ```

        **Step 2:**
        ``` bash
        cd booking_system_backend

        ```

        **Step 3:**
        ``` bash
        python -m venv .venv

        ```

        **Step 4:**
        ``` bash
        .venv\Scripts\activate

        ```

        **Step 5:**
        ``` bash
        python server.py

        ```

        !!! warning "DO NOT CLOSE THE TERMINAL WINDOW"

            For the following **Steps 6-9**, you will be required to open a second Terminal window. **Do not** close the Terminal previously used for **Steps 1-5**. This window will need to remain open and active for the duration of the lab.
        
        **Step 6:**
        After having completed all of **Steps 1-5**, open a new Terminal window by pressing the **Plus** button.

        <p align="left">
        <img src="https://colony-recorder.s3.amazonaws.com/files/2026-06-01/9693dc88-b869-4dd4-96d2-57f5d689ef8b/File_1fb9a0d9760b4b34970271f1c7fc4bb0_text_export.jpeg" alt="" width="600" />
        </p>

        <p align="left">
        <img src="https://colony-recorder.s3.amazonaws.com/files/2026-06-01/f6061f0e-30c8-4a9a-bb28-070277563c63/File_c80bec31c3ce4804a4b013fe419df79d_text_export.jpeg" alt="" width="600" />
        </p>

        **Step 7:**
        Within the new Terminal window, execute the following commands one-by-one in sequence:

        ```
        cd $HOME\galaxium-travels

        ```

        **Step 8:**
        ```
        cd booking_system_frontend

        ```

        **Step 9:**
        ```
        npm run dev

        ```

        <p align="left">
        <img src="https://colony-recorder.s3.amazonaws.com/files/2026-06-01/71540f93-ddab-4d54-bbba-4969a73da839/File_fd8e7412a078428aaeada831be555aa8_text_export.jpeg" alt="" width="600" />
        </p>

        You should see a *"Galaxium Travels is running!"* prompt returned to the console (amongst other messages) once the application has deployed successfully.

---

11. Open a web browser on your local machine and visit the following URL to access the Galaxium Travels application:

    !!! warning "WEB BROWSER"
        http://localhost:5173/

    <p align="left">
    <img src="https://colony-recorder.s3.amazonaws.com/files/2026-06-01/c3a8a5bb-9a57-4471-9e36-052baa221569/File_bd89b43df4a4435190abe1e56962204a_text_export.jpeg" alt="" width="600" />
    </p>

---

12. Once the Galaxium Travels site has loaded, click on **Flights**.

    <p align="left">
    <img src="https://colony-recorder.s3.amazonaws.com/files/2026-06-01/0197eae9-d0a5-49fb-bac9-c212a1fe0b5e/File_259715da67bd4610b6b4c06890714e3b_text_export.jpeg" alt="" width="600" />
    </p>

---

13. Click the **Book Now** button under any of the listed flights to test its functionality.

    <p align="left">
    <img src="https://colony-recorder.s3.amazonaws.com/files/2026-06-01/b01e69cd-2fd9-48f5-b88a-9add15f22157/File_327f11f3cb8a426ea6596675dc948f82_text_export.jpeg" alt="" width="600" />
    </p>

---

14. The web page will redirect to a **Sign In** panel. From this panel, we will be able to confirm (or troubleshoot) that the Bob-suggested email validation functionality is performing its intended job.

    - Within the *Name* field, supply a name of your choosing.
    - Within the *Email* field, try any combination of **improperly** formatted text that you like. Remember: you want to test that the tool is rejecting any inputs that don't match a standard email address. You can even test with a random string of characters like `$$$$$`, as an example.
    - The expected behavior of the function should be to reject any inproper addresses (`$$$$$`) and accept a standard address (`bienko@ibm.com`).
    
    </br>

    <p align="left">
    <img src="https://colony-recorder.s3.amazonaws.com/files/2026-06-01/0ff143f3-106c-452e-b224-0e56ab1ee1c5/File_0f0397e204e4480c84c1c74c9bb3e50e_text_export.jpeg" alt="" width="600" />
    </p>

    <p align="left">
    <img src="https://colony-recorder.s3.amazonaws.com/files/2026-06-01/cd99f6f1-4a1f-45b0-a422-de913f16b583/File_92f024001ef142779e931ab43d2e532d_text_export.jpeg" alt="" width="600" />
    </p>

    <p align="left">
    <img src="https://colony-recorder.s3.amazonaws.com/files/2026-06-01/efd525be-3acb-48c8-b0d6-1a80e5d48281/File_a3f80360cff646edae6a7bf64fc34b5f_text_export.jpeg" alt="" width="600" />
    </p>

## **iv. Improving retry logic**

---

Galaxium's backend can be slow to respond when fetching flight information. If you test on this, you'll discover that occasional timeouts are often the result. This underforming feature is an ideal candidate for showcasing the small, surgical fixes that literate coding excels at.

In the following section, you will apply literate coding to wrap the flight-fetching call in `retry` logic — and do so with minimal coding work on your part.

---

1. Within the Bob IDE, click the **Explorer** tab as you did previously to open the `galaxium-travels` project directory.

    - Drill down into the following directory: **/**`booking_system_frontend`**/**`src`**/**`pages`**/** `Flights.tsx`
    - Double-click on the **Flights.tsx** file to open it within the editor
    
    <p align="left">
    <img src="https://colony-recorder.s3.amazonaws.com/files/2026-06-01/47057405-de4e-4a16-b733-add7692b5acb/File_2c52e62792b84d49b1699cd7054f488e_text_export.jpeg" alt="" width="600" />
    </p>

---

2. Scroll down the contents of `Flights.tsx` until you reach **Line 43**, which an empty newline before the `loadFlights` function definition.

    === "Flights.tsx *(Unmodified)*"

        ``` tsx linenums="35" hl_lines="9"
            const term = searchTerm.toLowerCase();
            const filtered = flights.filter(
            (flight) =>
                flight.origin.toLowerCase().includes(term) ||
                flight.destination.toLowerCase().includes(term)
            );
            setFilteredFlights(filtered);
        }, [searchTerm, flights]);

        const loadFlights = async () => {
            setIsLoading(true);
            try {
            const data = await getFlights();
            setFlights(data);
            setFilteredFlights(data);
            } catch (error: any) {
            toast.error('Failed to load flights');
            console.error(error);
            } finally {
            setIsLoading(false);
            }
        };
        ```

---

3. Place your cursor at the beginning of the empty **Line 43** and then click the **magic wand icon** near the top of the editor (or press ++ctrl++ + ++i++) to activate *Literate Coding* mode.

    <p align="left">
    <img src="https://colony-recorder.s3.amazonaws.com/files/2026-06-01/541203d9-0a20-4ac7-a8dd-9a60d2334373/File_70a4346deb6344bc9763d181f6527c5a_text_export.jpeg" alt="" width="600" />
    </p>

---

4. Next, copy and paste the following statement directly into **Line 43** (be sure to include the empty lines before and after as well):
    ```

    The backend is unreliable at times. We need a retry logic here.
    
    ```
    ![](_attachments/literate-iv-4.png)

---

5. Click the **Generate** button (or press ++command++ + ++return++ as a shortcut).

    ??? warning "DEBUGGING: Failed to generate code from literate text."
        Some users have reported an error when attempting to generate code from literate text:
        `Failed to generate code from literate text. Please try again. Error: Each edit must have oldString and newString`

        In such instances, try deleting the lines of text you added or inserted. Then either paste the code back in or type the code in again manually. Make sure that the Literate Coding mode is active before doing so. Click the **Generate** button again when ready to proceed.

        Double-check that your instruction comment `validate email addresses` does *NOT* include "comment out" annotations like double `//` or `#` lines. Recent iterations of Bob's LLMs have been documented generating errors (related to newString and oldString) when encountering these comment lines. 

---

6. A ***diff* view** will render in-line at the code insertion point.

    - Code that IBM Bob is proposing to *remove* will be highlighted in red
    - Code that IBM Bob is proposing to *add* will be highlighted in green

    You may use the ++up++ and ++down++ arrows to cycle through all proposed code block changes. In this example, there are two such code blocks. You may inspect them and modify them individually, or even revert back all changes by clicking the *Undo All* button.

    Click the **Accept All** button to confirm Bob's recommendations and modify the `Flights.tsx` file.
    
    </br>
    ![](_attachments/literate-iv-6.png)

---

7. After confirming the *Literate Coding* generative AI recommendations, the file should look similar to the following.

    Cycle through the two tabs within this documentation to compare the *Modified* and *Unmodified* versions of the same relative code blocks.

    === "Flights.tsx *(Modified)*"

        ``` tsx linenums="35" hl_lines="10-20 25"
            const term = searchTerm.toLowerCase();
            const filtered = flights.filter(
            (flight) =>
                flight.origin.toLowerCase().includes(term) ||
                flight.destination.toLowerCase().includes(term)
            );
            setFilteredFlights(filtered);
        }, [searchTerm, flights]);

        const loadFlightsWithRetry = async (retries: number = 3, delay: number = 1000): Promise<Flight[]> => {
            try {
            return await getFlights();
            } catch (error: any) {
            if (retries <= 1) {
                throw error;
            }
            await new Promise((resolve) => setTimeout(resolve, delay));
            return loadFlightsWithRetry(retries - 1, delay * 2);
            }
        };

        const loadFlights = async () => {
            setIsLoading(true);
            try {
            const data = await loadFlightsWithRetry();
            setFlights(data);
            setFilteredFlights(data);
            } catch (error: any) {
        ```
    === "Flights.tsx *(Unmodified)*"

        ``` tsx linenums="35" hl_lines="9"
            const term = searchTerm.toLowerCase();
            const filtered = flights.filter(
            (flight) =>
                flight.origin.toLowerCase().includes(term) ||
                flight.destination.toLowerCase().includes(term)
            );
            setFilteredFlights(filtered);
        }, [searchTerm, flights]);

        const loadFlights = async () => {
            setIsLoading(true);
            try {
            const data = await getFlights();
            setFlights(data);
            setFilteredFlights(data);
            } catch (error: any) {
            toast.error('Failed to load flights');
            console.error(error);
            } finally {
            setIsLoading(false);
            }
        };
        ```
    ![](_attachments/literate-iv-7.png)

---

This particular change will not produce anything visible within the running application that you can test via the mocked-up website.

The point is to watch **targeted code generation** happen *in context* against real code, at which Bob has performed brilliantly.

!!! tip "BEFORE YOU MOVE ON"
    You can continue to tinker with this if you wish— but be mindful that changes you make and redeploy within the application will impact the website we are building. If you make changes, don't save them to the project directory code (and always be mindful of how many Bob tokens you are consuming and have remaining. Your account's token budget will only reset monthly for IBMers. Trial accounts will expire once the token budget is exhausted.

## **v. Key takeaways**

---

- Literate coding turns plain-language instructions, written in place, into precise code change
- The *diff* view keeps you in full control, allowing you to inspect and understand every change before it is committed to code
- The literate coding mode is most effective for focused, single-file modifications
- For multi-file or architectural changes, Code mode is the more appropriate tool for the job

</br>

For sweeping, multi-file changes or architectural restructuring, you will instead want to reach for IBM Bob's **Code mode**. The topic is covered extensively within <a href="https://ibm.github.io/bob-l3/delegate/2-1.md" target="_blank">**Module 2 | Delegate**</a>, which you will explore next.